# Database connection string for SQLite
DATABASE_URL="file:./dev.db"

# Express server port
PORT=5000

# Secret key for signing JSON Web Tokens (JWT)
JWT_SECRET="super_secret_key_for_attendance_system_2026_dev"
Algarithim@60
RdLKkiQa2WqXhLZV

# ── Email / SMTP Configuration ────────────────────────────────────────────────
# Option A: Use a named service (e.g. gmail, yahoo, hotmail, Mailgun, SendGrid)
# EMAIL_SERVICE=gmail

# Option B: Use custom SMTP host (e.g. Mailtrap for testing)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_SECURE=false

# Your SMTP credentials
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# The "from" address shown in outgoing emails
EMAIL_FROM="Attendance System <no-reply@attendance.edu>"